package model;


public class MainModel {
	private user[] users;
	
	
	public boolean checkUsers(String tUsername, String tPassword) {
		int length = users.length;
		boolean state = false;
		if (length == 0) {
			return false;
		}
		else {
			for (int )
		}
	}
}
